
if skill 'camping' < 120
    if not findtype "kindling" backpack
        overhead "Need more kindling" 34
        wait 5000
        dclicktype "dagger" backpack
        wft 500
        target self
        replay
    endif

if not timerexists 'hideTimer'
    createtimer 'hideTimer'
    settimer 'hideTimer' 11000
endif

if skill 'hiding' < 100
    if timer 'hideTimer' >= 11000
        useskill 'Hiding'
        settimer 'hideTimer' 0
        wait 500
    endif
endif

if skill 'camping' < 100
    if not findtype "campfire" ground 0 1 1 and findtype "kindling" backpack
        dclicktype "kindling" backpack
        wait 500
    endif
endif
wait 500
loop